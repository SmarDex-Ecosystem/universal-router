// SPDX-License-Identifier: UNLICENSED
pragma solidity 0.8.26;

contract MockUsdnProtocol {
    function getUsdn() external pure returns (address) {
        return address(0);
    }

    function getAsset() external pure returns (address) {
        return address(0);
    }
}
